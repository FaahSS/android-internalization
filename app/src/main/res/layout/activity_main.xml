<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/brasilLinearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center|top"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/paisTextView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/nomePais"
        android:textSize="@dimen/nomePais_textsize" />

    <ImageView
        android:id="@+id/paisImageView"
        android:layout_width="@dimen/larguraBandeira"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:contentDescription="@string/imagemBandeira"
        app:srcCompat="@drawable/bandeira" />

    <ImageView
        android:id="@+id/comidaImageView"
        android:layout_width="@dimen/larguraComida"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:contentDescription="@string/imagemComida"
        app:srcCompat="@drawable/comida" />


    <TextView
        android:id="@+id/comidaTextView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/nomeComida"
        android:textSize="@dimen/nomeComida_textsize" />

</LinearLayout>